name: CI

on:
  push:
    branches:
      - "**"
    tags:
      - "v*"
  pull_request:

jobs:
  doctest:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.12", "3.13", "3.14"]
        include:
          # Snapshot comparison is only meaningful on the reference platform.
          # On macOS and Windows, matplotlib produces slightly different SVG
          # floating-point values, so we use doctest-without-snapshots there.
          - os: ubuntu-latest
            doctest_target: doctest
          - os: macos-latest
            doctest_target: doctest-without-snapshots
          - os: windows-latest
            doctest_target: doctest-without-snapshots

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install make (Windows)
        if: runner.os == 'Windows'
        run: choco install make

      - name: Install dependencies
        run: pip install -e .[dev]

      - name: Run doctests
        run: cd docs && make ${{ matrix.doctest_target }}
        env:
          MPLBACKEND: Agg
